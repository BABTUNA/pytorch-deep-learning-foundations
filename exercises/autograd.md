# Autograd exercises

- Differentiate a quadratic at three points and check the result by hand.
- Follow `grad_fn` through a two-layer expression.
- Show why parameter updates belong inside `torch.no_grad()`.
- Compare accumulated gradients with gradients cleared after every batch.
