# Checkpoint exercises

Save a model after validation improves, reload it into a fresh model, and check
that both models return the same scores. Then resume an optimizer and verify
that its learning rate and momentum state were restored.
