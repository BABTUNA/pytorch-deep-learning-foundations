# Results

The CNN improved validation accuracy over the MLP baseline. Augmentation and
batch normalization made validation loss more stable, while dropout helped most
after the model width increased. Results are illustrative course runs rather
than a benchmark.
