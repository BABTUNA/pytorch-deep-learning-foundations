# Course retrospective

The useful shift was moving from notebook-only experiments to small reusable
data, training, validation, and checkpoint helpers. The next step would be
running several seeds and adding TensorBoard logging instead of relying on one
comparison table.
