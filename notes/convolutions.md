# Convolution notes

The output size depends on input size, kernel, padding, stride, and dilation.
Start with padding that preserves width and height, then use pooling to reduce
both. Visualize a few learned filters before making the network deeper.
