# Setup

Create a Python 3.10 virtual environment and install `requirements.txt`.
Notebooks assume the repository root is the working directory. CPU execution
is enough for the early exercises; the CNN experiments can also use CUDA.
