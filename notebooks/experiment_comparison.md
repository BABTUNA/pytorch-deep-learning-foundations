# Experiment comparison

Load `experiments/comparison.csv`, sort runs by validation accuracy, and plot
loss against accuracy. The regularized CNN with augmentation performed best,
but its advantage over the SGD run is small enough to repeat with more seeds.
