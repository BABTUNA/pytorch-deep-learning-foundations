# Regularization runs

Compare the same CNN with no regularization, dropout only, batch normalization
only, and both together. Keep the split, seed, optimizer, and epoch budget
fixed. Augmentation gets a separate run because it changes the input data.
