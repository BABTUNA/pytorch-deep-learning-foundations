# MLP baseline

First run: one hidden layer with 128 units, ReLU, Adam at `1e-3`, and batches
of 64. Track training and validation loss for 20 epochs before changing the
architecture. Save the random seed with every result.
